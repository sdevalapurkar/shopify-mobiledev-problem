import React, { Component } from 'react';
import {
    Text,
    View
} from 'react-native';
import Router from './Router.js';

class App extends Component {
    render() {
        return (
            <Router />
        );
    }
}

export default App;
